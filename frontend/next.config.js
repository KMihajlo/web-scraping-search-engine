/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  experimental: {
    // appDir disabled for now; using pages router for simplicity
  },
}

module.exports = nextConfig;

